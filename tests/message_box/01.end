\layout(\Box(\MessageBox()))
\layout(\ExecButton("full"))

config.text.periodWait = true
config.text.speed = 100

aaa
bbb
*ccc*
{ddd|eee}
fff@ggg
hhh\
iii
\wait('snapshot')

あああ。いいい@ううう
えええ。\おおお
\wait('snapshot')

「あいうえお」
\wait('snapshot')

名前「かきくけこ」
\wait('snapshot')

\layout(\History)
\wait('snapshot')
